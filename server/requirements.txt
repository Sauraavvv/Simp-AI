fastapi>=0.115
uvicorn[standard]>=0.30
# One client for every OpenAI-compatible provider -- see llm.py.
openai>=1.40
python-dotenv>=1.0
httpx>=0.27
# Optional tracing -- inert unless LANGSMITH_TRACING is set.
langsmith>=0.4
pymongo>=4.0
dnspython>=2.0
